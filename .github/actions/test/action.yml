name: 'Hello World'
description: 'Greet someone'
inputs:
  path:  # id of input
    description: 'Where to checkout files to?'
    required: true
# outputs:
#   random-number:
#     description: "Random number"
#     value: ${{ steps.random-number-generator.outputs.random-number }}
runs:
  using: "composite"

  # using: "docker"
  # image: node:16

  steps:
    - run: echo "Hello world (3)"
      shell: bash
    - run: pwd
      shell: bash
    - run: ls -la
      shell: bash
    - run: |
        pwd
        mkdir -p ${{ inputs.path }}
        cd ${{ inputs.path }}
        pwd
        touch MADE_IN_MY_ACTION.txt
        ls -la
      shell: bash
    - run: pwd
      shell: bash
    - run: ls -la
      shell: bash
    # - id: random-number-generator
    #   run: echo "random-number=$(echo $RANDOM)" >> $GITHUB_OUTPUT
    #   shell: bash
    # - run: echo "${{ github.action_path }}" >> $GITHUB_PATH
    #   shell: bash
    # - run: goodbye.sh
    #   shell: bash
