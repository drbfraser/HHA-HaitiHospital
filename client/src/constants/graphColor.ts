const ALPHA_VALUE = 0.5;

const GRAPH_COLOR = [
  `rgba(230, 0, 73, ${ALPHA_VALUE})`,
  `rgba(11, 180, 255,  ${ALPHA_VALUE})`,
  `rgba(80, 233, 145, ${ALPHA_VALUE})`,
  `rgba(230, 216, 0, ${ALPHA_VALUE})`,
  `rgba(155, 25, 245, ${ALPHA_VALUE})`,
  `rgba(255, 163, 0, ${ALPHA_VALUE})`,
  `rgba(220, 10, 180, ${ALPHA_VALUE})`,
  `rgba(179, 212, 255, ${ALPHA_VALUE})`,
  `rgba(0, 191, 160, ${ALPHA_VALUE})`,
];

export default GRAPH_COLOR;
