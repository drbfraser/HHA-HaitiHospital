import * as React from "react";
// import { Link } from "react-router-dom";

interface Props {}

export const MessageBoardMain: React.FC<Props> = () => {
    return (
        <div>
            <div>this is a Case message board page</div>
            {/*<Link to={`/posts/${postId}`}>go to post 1</Link>*/}
        </div>
    );
};